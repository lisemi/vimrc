
.. code-block:: c
    Instructions：
    1.install vundle
	    git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
    2.Vimrc rename the copy to the user directory
    3.Install the plugin
